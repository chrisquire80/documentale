# Database
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=documentale

# Security
SECRET_KEY=yoursecretkeyhere
AUTO_USER_EMAIL=admin@example.com
WATCH_DIR=/app/auto_ingest
# Comma-separated list if you want to support multiple extensions in env (optional improvement)
# For now, we'll keep it simple as it is just an example.

# Gemini
GEMINI_API_KEY=your_gemini_api_key
GEMINI_ENABLED=true
